#ifndef LOGIC_H
#define LOGIC_H
//the above are called as header guards and they prevent the same file from being including
//again thus preventing any compilation errors

// a simple function we want to test
int add(int a, int b);

#endif // this will end the header block that started with ifndef